# 千年晤旅 · 青绿山水换肤

将本包中的 config.py、views/home.py、assets/home_hero_v2.png 放到原项目相同位置。替换前请备份原文件；不要删除其他项目文件。

本包沿用本次对话中取得的 GitHub 项目版本。若你或朋友之后修改过 config.py 或 views/home.py，请先比较合并，勿直接覆盖。

改动：首页使用新横版山水画；全站共享导航、按钮、表单、聊天卡片采用米白和青绿样式；保留已有数据、人物图片、推荐内容、状态和页面跳转。地图内部与导出票根中的原始美术保持不变。

上述为上传换肤包的说明。第二轮已完成下列布局修改，未推送、未发布：

- 单列分组导航与八步游历进度；当前页标记和开启游历主入口。
- 统一人物卡片，首页四位重点人物，其余通过查看更多展开，全部原按钮保留。
- 路线与档案纵向站点时间轴；聊天人物/地点信息栏、纸色人物消息与浅青色用户消息。
- 档案按山水行迹、光影与纪念、纸上心事组织；所有合影、票根和随笔完整展示。
- config.py 为唯一 CSS 来源，home.py 不再嵌入覆盖样式；Streamlit 原生主题同步为青绿。
- 首页 picture 优先加载约 161 KiB WebP，不支持的浏览器回退约 2.3 MiB PNG。原图保留在 assets/，static/ 存放专用公开图片；不要往 static/ 放私密文件。
- 所有首页动画支持 prefers-reduced-motion；手机断点采用单列卡片、两行步骤条。

验证：Python 编译、Git diff 格式、原离线全链路冒烟测试、扩展隔离数据库回归均通过。扩展回归覆盖 17 位人物与原操作、小传、检索选人偏好、GPS 成功、含 5 张图的档案与原下载/删除入口。core/ 与 data/ 共 18 个 Python 文件和第二轮开始前 checkpoint 字节一致。

尚未完成最终桌面/手机视口的视觉验收：浏览器自动审批连续超时。不要将静态响应式样式已实现等同于手机端视觉测试通过。在线 AI 未调用验证，当前 Preview 使用离线演示。

运行：AI_DISABLED=1 .venv/bin/streamlit run app.py --server.address 127.0.0.1 --server.port 8501。本轮测试版本为 Streamlit 1.51.0；主题与静态服务配置修改后需重启。requirements.txt 保持原样。
